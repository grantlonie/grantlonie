import { baseTheme, extendTheme } from '@chakra-ui/react'

const theme = {
  colors: {
    primary: '#ff9f56ff',
    background: '#EEEEEE',
    black: '#222831',
    white: '#ffeec7ff',
  },
}

export default extendTheme(theme)
export { baseTheme }
