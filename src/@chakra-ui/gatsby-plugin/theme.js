import { baseTheme, extendTheme } from '@chakra-ui/react'

const theme = {
  colors: {
    primary: '#ff6f00',
    background: '#FBF0D6',
    black: '#4E4E4E',
    white: '#FFEEC6',
  },
}

export default extendTheme(theme)
export { baseTheme }
